version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
    nodejs: "20"
  jobs:
    pre_build:
      - npm ci
      - npm run build

mkdocs:
  configuration: mkdocs.yml

python:
  install:
    - requirements: documentation/requirements.txt
