version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
    nodejs: "20"
  jobs:
    pre_build:
      - node scripts/generate-docs.js

mkdocs:
  configuration: mkdocs.yml

python:
  install:
    - requirements: documentation/requirements.txt

search:
  ranking:
    index.md: 1.0
    getting-started.md: 0.9
    api/index.md: 0.8
